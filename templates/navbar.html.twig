<nav class="navbar text-color-black navbar-expand-lg navbar-light bg-navbar">
    <div class="container-fluid mt-2 mb-1">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <div class="row align-items-center">
                <div class="navbar-nav mx-auto my-auto mt-2">
                    <div class="col-lg-4 col-md-auto pb-3 nav-item">
                        <a class="navbar-brand" href="/">
                            <img src="{{ asset('build/images/logo-beeznessly.png') }}" alt="" width="100">
                        </a>
                    </div>
                    <div class="col-lg-auto nav-item text-color-black">
                        <a class="nav-link" href="{{ path('home_experts')}}">Consulter les experts</a>
                    </div>
                    <div class="col-lg-auto nav-item">
                        <a class="nav-link" href="{{ path('home_ebooks')}}">Télécharger les Ebooks</a>
                    </div>
                    <div class="col-lg-auto nav-item">
                        <a class="nav-link" href="{{ path('contact')}}">Contacter Beeznessly</a>
                    </div>
                    <div class="col-lg-2 nav-item">
                        {% if app.user is null %}
                        <a href="{{path('app_login')}}">
                            <button type="button" class="btn btn-connexion">Connexion</button>
                        </a>
                        {% else %}
                        <a href="{{path('app_logout')}}">
                            <button type="button" class="btn btn-connexion">Déconnexion</button>
                        </a>
                        {% endif %}
                    </div>
                    <div class="col-lg-2 nav-item">
                        {% if app.user is null %}
                        <a href="{{path('app_register_expert')}}">
                            <button type="button" class="btn btn-inscription ">S'inscrire</button>
                        </a>
                        {% else %}
                        {% if app.user.roles[0] == "ROLE_EXPERT" %}
                        <a class="nav-link navbar-brand" href="{{path('expert_index')}}">
                            <i class="fas fa-user-alt user-icon"></i>
                        </a>
                        {% endif %}
                        {% if app.user.roles[0] == "ROLE_ENTREPRENEUR" %}
                        <a class="nav-link navbar-brand" href="{{path('entrepreneur_index')}}">
                            <i class="fas fa-user-alt user-icon"></i>
                        </a>
                        {% endif %}
                        {% endif %}
                    </div>
                </div>
            </div>
        </div>
    </div>
</nav>