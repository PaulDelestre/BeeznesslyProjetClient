{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="button" class="btn btn-inscription mb-3"><i class="far fa-arrow-alt-circle-right"></i> {{ button_label|default('Save') }}</button>
{{ form_end(form) }}
